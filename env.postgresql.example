# PostgreSQL 환경 변수 예시
# 이 파일을 .env로 복사하여 사용하세요

APP_NAME="Todo RESTful API (PostgreSQL)"
APP_VERSION=1.0.0
DEBUG=True

# PostgreSQL 연결 정보 (Docker 사용 시)
DATABASE_URL=postgresql+psycopg2://todo_user:1234@127.0.0.1:5433/todo_db

# PostgreSQL 연결 정보 (로컬 설치 시)
# DATABASE_URL=postgresql+psycopg2://todo_user:1234@127.0.0.1:5432/todo_db

HOST=0.0.0.0
PORT=8000
